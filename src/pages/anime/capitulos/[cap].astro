---
// import type { GetStaticPaths } from "astro";
export const prerender = false;
const { cap } = Astro.params;
cap;
// export const getStaticPaths = (() => {
//     return [{ params: { cap: "kaijuu-8-gou-episode-1" } }];
// }) satisfies GetStaticPaths;
---
